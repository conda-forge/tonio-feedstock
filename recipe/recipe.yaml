schema_version: 1

context:
  version: "0.9.6"

package:
  name: tonio
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/t/tonio/tonio-${{ version }}.tar.gz
    sha256: 6ba5f2f3c42da9bc4efd7091e20537fa4e259e0b7f36ab454f35b6db2a7f2053

build:
  number: 0
  skip:
    - win
    - match(python, "<3.14")
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml

requirements:
  build:
    - python
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
    - maturin >=1.8,<1.9
  host:
    - maturin >=1.8,<1.9
    - pip
    - python
  run:
    - python

tests:
  - python:
      imports: tonio
      pip_check: true

about:
  license: BSD-3-Clause
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  homepage: https://github.com/gi0baro/tonio
  repository: https://github.com/gi0baro/tonio
  summary: A multi-threaded async runtime for Python.

extra:
  recipe-maintainers:
    - davidbrochart
